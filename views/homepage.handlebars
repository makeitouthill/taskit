{{nav}}
<div class="container">
    <div class="service-container">
        {{#each services}}
        <div class="service">
            <h2>{{service_name}}</h2>
            <p>{{description}}</p>
            {{> jobOfferings job_offers}}
        </div>
        {{/each}}
    </div>
</div>



